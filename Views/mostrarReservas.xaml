<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AuroraApp_MAUI.Views.mostrarReservas"
             xmlns:viewModels="clr-namespace:AuroraApp_MAUI.ViewModels"
            Title="Tus Reservas">

  
    <CollectionView x:Name="reservasCollection"
                      ItemsSource="{Binding reservas}" >

        
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
        </CollectionView.ItemsLayout>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout>
                    <Label Text="{Binding nombre}" FontSize="20"/>
                    <Label Text="{Binding numPersonas}" FontSize="14"/>
                    <Label Text="{Binding telefono}" FontSize="14"/>
                    <Label Text="{Binding horaLLegada}" FontSize="14" TextColor="Silver"/>
                    <Label Text="{Binding date}" FontSize="14" TextColor="Silver"/>
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
        
    </CollectionView>
</ContentPage>
