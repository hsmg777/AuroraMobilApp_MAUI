<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:AuroraApp_MAUI.ViewModels"
             x:Class="AuroraApp_MAUI.Views.mostrarReservas"
             Title="Tus Reservas">

    <CollectionView x:Name="reservasCollection" ItemsSource="{Binding ReservasList}" >

        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
        </CollectionView.ItemsLayout>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout>
                    <Label Text="{Binding nombre}" FontSize="20"/>
                    <Label Text="{Binding numPersonas}" FontSize="14"/>
                    <Label Text="{Binding telefono}" FontSize="14"/>
                    <Label Text="{Binding horaLlegada}" FontSize="14" TextColor="Silver"/>
                    <Label Text="{Binding fecha}" FontSize="14" TextColor="Silver"/>
                    <Button Text="Editar" Command="{Binding BindingContext.EditCommand, Source={x:Reference Name=reservasCollection}}" CommandParameter="{Binding .}" />
                    <Button Text="Eliminar" Command="{Binding BindingContext.DeleteCommand, Source={x:Reference Name=reservasCollection}}" CommandParameter="{Binding .}" />
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>
</ContentPage>
